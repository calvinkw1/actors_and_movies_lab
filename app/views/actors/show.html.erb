<h1>Showing <%= @actor.name %></h1>


<h3>Movies</h3>

<% flash.each do |name, msg| -%>
  <%= content_tag :div, msg, class: name %>
<% end -%>

<% @actor_movies.each do |movie| %>
  <p>
    <%= link_to "#{movie.title}", movie %>
    <%= button_to "Remove", remove_movie_path(@actor, movie), :method => :delete %>
  </p>
<% end %>

<p>
  <%= link_to "Edit Actor", edit_actor_path %>
</p>


Add a New Movie: 
  <%= form_tag add_movie_path do |f|%>
    <select name="movie_id">
      <% @movies.each do |flick| %>
      <option value="<%=flick.id%>"><%=flick.title %></option>
      <% end %>
    </select>
    <input type="submit" value="Add">
  <% end %>


<p>
  <%= link_to "Back to all Actors", actors_path %>
</p>